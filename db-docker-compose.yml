version: '3'

services:

  mongo:
    image: mongo:latest
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: quests
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - 0.0.0.0:27017:27017
